## Imported Claude Cowork project instructions

Follow best practices for tactical RPGs. Do not use incredibly short variable names unless for specific purposes like dx/dy, and use up to date dependencies if required. Make sure code is linted and formatted properly, which may require setup

Never use `codex/` prefixed branch names, only semantically useful naming conventions.
